window.addEventListener("DOMContentLoaded",function(){document.getElementById("anonymizer").addEventListener("submit",event=>{})}),onsubmit=event=>{event.preventDefault(),(userinput=document.querySelector('[name="urlanon"]').value).startsWith("http")||(userinput="http://"+userinput);try{url=new URL(userinput),toAnonymize=url.hostname,parts=toAnonymize.split("."),domain=(domain=parts[parts.length-2]).length<8?domain[0]+"..."+domain[domain.length-1]:domain.slice(0,3)+"..."+domain.slice(domain.length-3,domain.length)}catch(err){log.innerHTML="<p><mark>Keine gültige URL.</mark> Bitte Eingabe in der Form <pre>https://example.com/{path/to/file.html}</pre></p>"+err+"</p>"}parts[parts.length-2]=domain,newDomain=url.protocol+"//"+parts.join(".")+url.port+url.pathname+url.search+url.hash,navigator.clipboard.writeText(newDomain);event="<p>In die Zwischenablage kopiert:</p>";event+="<mark>"+newDomain+"</mark>",log.innerHTML=event};