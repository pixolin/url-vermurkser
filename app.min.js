window.addEventListener("DOMContentLoaded",function(){document.getElementById("anonymizer").addEventListener("submit",onsubmit)}),onsubmit=n=>{n.preventDefault();n=document.querySelector('[name="urlanon"]').value;try{url=new URL(n),toAnonymize=url.hostname,parts=toAnonymize.split("."),domain=(domain=parts[parts.length-2]).length<8?domain[0]+"..."+domain[domain.length-1]:domain.slice(0,3)+"..."+domain.slice(domain.length-3,domain.length)}catch(n){log.innerHTML="<p><mark>Keine gültige URL.</mark> Bitte Eingabe in der Form <pre>https://example.com/{path/to/file.html}</pre></p>"+n+"</p>"}parts[parts.length-2]=domain,newDomain=url.protocol+"//"+parts.join(".")+url.port+url.pathname+url.search+url.hash,navigator.clipboard.writeText(newDomain);n="<p>In die Zwischenablage kopiert:</p>";n+="<mark>"+newDomain+"</mark>",log.innerHTML=n};